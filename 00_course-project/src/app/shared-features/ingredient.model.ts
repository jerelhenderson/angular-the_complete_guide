export class Ingredient {
  // public name: string;
  // public amount: number;
  
  // 'public' creates properties "name" and "amount" and assigns them as values to the Ingredient component
  constructor(public name: string, public amount: number) {
    // this.name = name;
    // this.amount = amount;
  }
}
